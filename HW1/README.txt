This program reads in a text file, parses out words, and fits them into a cache with a size determined by a command line. Uses dynamic memory allocation and prohbits use of brackets.

Input is:  <cache-size> <input_file>

Example input:

./a.out 10 input.c
